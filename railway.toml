[build]
builder = "NIXPACKS"
buildCommand = "cd \"milestone 3/backend\" && npm install"

[deploy]
startCommand = "cd \"milestone 3/backend\" && npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
